import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "xyz.cqn.cqntrack",
  appName: "cqntrack",
  webDir: "../cqntrack_fe/dist-mobile",
};

export default config;
